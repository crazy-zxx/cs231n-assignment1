# at first, your virtual environment need install python 3.8
# then, conda install -c conda-forge notebook  (I use this)  or  pip install notebook
# last, pip install -r requirements.txt    (because some package conda don't have)
# The packages below are trimmed down,
# some packages provided by the original site conflict with jupyter dependencies
chardet
cycler
future
gitdb2
GitPython
idna
imageio
kiwisolver
matplotlib
nbdime
numpy
pexpect
Pillow
ptyprocess
qtconsole
requests
scipy
smmap2
urllib3
widgetsnbextension
